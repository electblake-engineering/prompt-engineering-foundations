# Claude Context

@AGENTS.md

Use this repo as a static-exported Next.js 16 docs site:

- Local dev: `npm run dev`
- Export build: `npm run build`
- Local static preview: `npm start`
- Deployment: GitHub Actions to GitHub Pages (`.github/workflows/deploy.yml`)

For deployment and architecture details, see `references/docs-site-deployment.md`.
